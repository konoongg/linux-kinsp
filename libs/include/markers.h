#ifndef LK_MARKERS_H
#define LK_MARKERS_H

/*
 * SINGLTON - marker for public API functions of entities that are
 * singletons (one global instance per program, e.g. logger, config).
 * Has no semantic effect, used only to document ownership of the API.
 */
#define SINGLTON

#endif
